# Current State
- **Topic:** New arrival showcase carousel
- **Slug:** showcase-carousel
- **File path:** showcase-carousel/index.html
- **Version:** 1.1.0
- **Status:** Done
- **Key decisions:**
  - Built fresh using `hyperframes init --tailwind`.
  - 16:9 aspect ratio.
  - Used Tailwind v4 with `@theme` overrides hooked into `window.__vars`.
  - Exported all editable values to `window.__vars` directly for easy modification (text, colors, images, typography, timings).
  - Ensured GSAP timelines match the deterministic duration constraints.
  - Extracted header and background sections into independent sub-compositions, resolving dynamic DOM injection limits.
  - Clean `npm run check` with no remaining lints/errors.
- **Known issues:** None.
