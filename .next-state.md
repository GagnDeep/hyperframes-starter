# Next State

- Backlog:
  - Extract the text scramble/reveal animation into a reusable module.
  - Support multiple animation themes (pop-in, slide, fade).
  - Add particle effects for the reveal moment.
  - Implement a true typing or text-scramble effect for the code itself instead of a simple pop-in.
  - Add custom transition shaders between the scenes.
